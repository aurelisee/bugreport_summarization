A failing test case is created to illustrate the bug, after calculating the fuzz factor, the result represent a bigger value. Each individual hunk must have the fuzz factor in order to fix this bug. Patch is created, and now a fuzz factor si set and used individually for each hunk. Beside, some comments are also changed. A test case is created to find the fuzz factor. another bug is the duplicate of this bug.