An empty collection is returned when a sequence on PostgreSQL is queried via org.eclipse.datatools.modelbase.sql.schema.Schema#getSequences(). The patches does mention the purpose and changes it applies, advised to include such information for future patches. another problem with branching is that on add the changes go to local master while other changes remain in bug branch, this might cause some merging problems later.