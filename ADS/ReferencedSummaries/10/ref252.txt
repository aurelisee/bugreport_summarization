To fix bug and provide input to the compare editor, two REST operations need to be provided, git status and diff resource. There are similar implementation to assist this bug, bug 334766 provide a nicer API for diff, and a file is to ecorate resources in the nivigator. Fixing another bug will be good to this one, not only need git commands, but also need a way to add Git uris to get file response. The code responsible for the diff operation and add git uris to a file is created. A wiki page is created with a sketch of the ui. The status are discuss to be include in it, falls into two category, staged and unstaged.