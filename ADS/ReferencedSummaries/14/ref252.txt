When the mysqlcatalogdatabase.refresh() is called, he schema is suspend by the database and is not refreshed. The listeners received the message but when passing it, it becomes stale. There are some flags seem to unused, and other flags don't function well. The patch should fix the flags and then the bug will be fixed. The patch need more tested, and the release is set to future. Additional modification on super class is add, and the bug is fixed, it works correctly.