When start the contributed sourceprovider, it's always cause problems. It will be better if the workbench can initialize as soon as possible before other bundles. There are two ways to fix it, first is protect the code not to load the other bundles and the other is initialize the default services. A patch is created to reproduce the bug. The source provider can be initialized between the workbench and other windows. A patch is released and is for RC1, but later the fix caused another bug.1. load ' org.eclipse.jdt.ui ' from cvs with tag=v20100316-0800
2. start a target workspace == > boom!
